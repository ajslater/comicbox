jobs:
  build:
    machine:
      image: ubuntu-2004:current
    environment:
      DOCKER_CLI_EXPERIMENTAL: enabled
      DOCKER_BUILDKIT: 1
    steps:
      - checkout
      - run:
          command: docker compose build comicbox-builder
          name: Build Builder
      - run:
          command: ./docker/docker-compose-exit.sh comicbox-lint
          name: Comicbox Lint
      - run:
          command: ./docker/docker-compose-exit.sh comicbox-test
          name: Comicbox Test
      - store_test_results:
          path: test-results/pytest
      - store_artifacts:
          path: test-results/coverage
      - run:
          command: ./docker/docker-compose-exit.sh comicbox-build
          name: Build Comicbox Dist
      - persist_to_workspace:
          paths:
            - ./README.md
            - ./ci
            - ./dist
            - ./pyproject.toml
          root: .
  deploy:
    docker:
      - image: cimg/python:3.10.0
    steps:
      - attach_workspace:
          at: .
      - run:
          command: ./ci/publish-pypi.sh
version: 2.1
workflows:
  main:
    jobs:
      - build:
          filters:
            branches:
              only:
                - develop
                - circleci
                - /.*-ci/
                - main
                - release
                - release-candidate
      - deploy:
          filters:
            branches:
              only:
                - main
                - release
                - release-candidate
          requires:
            - build
  version: 2.1
